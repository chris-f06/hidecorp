{% extends 'base.html.twig' %}

{% block title %}Hello TodoController!{% endblock %}

{% block body %}
    <div class="mt-5">
        <div class="d-flex justify-content-between align-items-center">
            <h1>Liste de tâche</h1>
            <a href="{{ path('app_todo_add') }}" class="btn btn-info">Ajouter une tâche</a>
        </div>
        {{ include('parts/todo/_table.html.twig', { field: { 'todos' : todos } }) }}
    </div>
    <div class="mt-5">
        <h1>Terminés</h1>
        {{ include('parts/todo/_table.html.twig', { field: { 'todos' : dones } }) }}
    </div>
{% endblock %}
